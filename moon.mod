name = "guo-dong-123/moon-trace"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/guo-dong-123/moon-trace"

license = "MIT"

keywords = ["trace", "observability", "agent", "debugging", "tui"]

preferred_target = "native"

description = "Agent execution trace observability and debugging toolkit for MoonBit"

import {
  "moonbitlang/x@0.5.4",
  "moonbitlang/async@0.20.1",
}
